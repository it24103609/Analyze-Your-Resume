# Analyze Your Resume

Run `npm install` and then `npm start`.

Open `http://localhost:3000` to start the ATS Analyzer experience.

The site has two pages:

- `index.html` for resume upload
- `analysis.html` for the ATS analysis dashboard

Supported upload formats:

- PDF
- DOC
- DOCX
